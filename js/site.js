const ring=document.querySelector(".dot-ring");if(ring){const order=Array.from({length:28},(_,i)=>i);for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=new Map(order.map((position,delay)=>[position,delay]));for(let i=0;i<28;i++){const dot=document.createElement("i");dot.className="dot";dot.style.setProperty("--a",`${i*(360/28)}deg`);dot.style.setProperty("--d",`${rank.get(i)*.035}s`);ring.appendChild(dot)}}
document.querySelectorAll("[data-year]").forEach(el=>el.textContent=new Date().getFullYear());
const supporters=document.querySelector(".supporter-grid");if(supporters){for(let i=0;i<10;i++){const el=document.createElement("div");el.textContent="Company name";supporters.appendChild(el)}}
const patrons=document.querySelector(".patron-names");if(patrons){patrons.className="name-grid";for(let i=0;i<8;i++){const el=document.createElement("p");el.textContent="Your name here";patrons.appendChild(el)}}
const members=document.querySelector(".member-names");if(members){members.className="name-grid";for(let i=0;i<16;i++){const el=document.createElement("p");el.textContent="Your name here";members.appendChild(el)}}
const form=document.querySelector("#waitlist-form");if(form){form.addEventListener("submit",async e=>{e.preventDefault();const msg=form.querySelector(".form-message");const data=new FormData(form);if(data.get("website"))return;data.set("source","Website waitlist");msg.textContent="Joining…";try{await fetch("https://script.google.com/macros/s/AKfycbwifZsijTxyy1liD63fnC0avolhMTNM_GlJsosAMGw4V_M2JX_IL0wsuyl-U8rS3yox/exec",{method:"POST",mode:"no-cors",body:new URLSearchParams(data)});form.reset();msg.textContent="You’re on the list. We’ll keep you updated as the scheme takes shape."}catch{msg.textContent="We couldn’t add you just now. Please try again or email hello@payrollgivingscheme.com."}})}
